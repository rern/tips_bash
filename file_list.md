### File list
```sh
# files and directories
itemarray=( /path/to/dir/* )
item0=${itemarray[0]}

# files
filearray=( /path/to/dir/*.* )
file0=${filearray[0]}

# directories
dirarray=( /path/to/dir/*/ )
dir0=${dirarray[0]}
```
